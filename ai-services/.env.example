# PackCheck AI Vision & OCR Service Environment Configuration (Example)

# Execution Port (passed by cloud platform or local Docker container)
PORT=8000

# Environment Mode (development / production)
ENVIRONMENT=development

# Service Name
SERVICE_NAME=PackCheck AI Vision Service
